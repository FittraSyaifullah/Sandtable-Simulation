import { BookOpenCheck, Database, Gauge, ShieldCheck } from "lucide-react";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";

export type CalibrationCase = { id: string; slug: string; title: string; historical_period: string; description: string; dataset_version: string; expected_ranges: Record<string, [number,number]>; source_manifest: { title?: string; type?: string }[] };
type Props = { open:boolean; onOpenChange:(open:boolean)=>void; activeDataset:string; calibrationCases:CalibrationCase[] };

export function MethodDialog({ open, onOpenChange, activeDataset, calibrationCases }: Props) {
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-4xl rounded-[28px] border-white/10 bg-[#0c0c0c] text-stone-100"><DialogHeader><DialogTitle>Method & evidence registry</DialogTitle><DialogDescription className="text-stone-500">Versioned provenance and explanatory calibration references for an inspectable educational model.</DialogDescription></DialogHeader><div className="grid gap-3 md:grid-cols-3"><MethodCell icon={Gauge} title="Weekly strategic ticks" text="One canonical replay resolves movement, adjacency, supply, strength, and objective state."/><MethodCell icon={Database} title="240 seeded samples" text="The same force layer reruns with deterministic sample seeds to produce central ranges."/><MethodCell icon={BookOpenCheck} title="Versioned provenance" text={`Model unified-force-0.3 · dataset ${activeDataset} · as of 2025-01-15.`}/></div><section className="rounded-2xl border border-white/8 bg-white/[.025] p-4"><div className="flex items-center justify-between"><div><p className="text-sm font-medium">Aggregate Capability Reference 2025.1</p><p className="mt-1 text-[10px] text-stone-600">World Bank macro indicators · governed normalized capability inputs</p></div><span className="rounded-full bg-emerald-500/10 px-2 py-1 text-[9px] uppercase tracking-wider text-emerald-300">Active · 8 records</span></div><p className="mt-3 text-xs leading-relaxed text-stone-500">Governed local records remain available when Supabase reference queries fail. Nation values are aggregate educational inputs and are not current operational intelligence.</p></section><section><div className="mb-2 flex items-center justify-between"><p className="text-[10px] uppercase tracking-[.18em] text-stone-500">Calibration references</p><span className="text-[9px] text-stone-600">Explanatory · not automatically executed</span></div><div className="grid gap-3 md:grid-cols-2">{calibrationCases.length?calibrationCases.map(item=><div key={item.id} className="rounded-2xl border border-white/8 bg-white/[.025] p-4"><p className="text-sm font-medium text-stone-200">{item.title}</p><p className="mt-1 text-[9px] uppercase tracking-wider text-yellow-300">{item.historical_period}</p><p className="mt-3 text-xs leading-relaxed text-stone-500">{item.description}</p><div className="mt-3 flex flex-wrap gap-1.5">{Object.entries(item.expected_ranges).map(([metric,range])=><span key={metric} className="rounded-full border border-white/8 bg-black/30 px-2 py-1 font-mono text-[9px] text-stone-400">{metric.split("_").join(" ")} · {range[0]}–{range[1]}</span>)}</div><p className="mt-3 text-[9px] text-stone-600">Source: {item.source_manifest.map(source=>source.title).filter(Boolean).join(", ") || "Governed registry"}</p></div>):<div className="md:col-span-2 rounded-2xl border border-dashed border-white/10 p-6 text-center text-xs text-stone-600">Calibration registry is temporarily unavailable.</div>}</div></section><div className="flex gap-3 rounded-2xl border border-yellow-400/20 bg-yellow-400/[.05] p-4 text-xs leading-relaxed text-stone-400"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0 text-yellow-300"/><p><strong className="text-yellow-100">Responsible-use boundary.</strong> Sandtable excludes real unit locations, targeting, strike planning, weapons optimization, attack routes, detection evasion, and operational recommendations. Calibration references cannot establish predictive validity.</p></div></DialogContent></Dialog>;
}
function MethodCell({icon:Icon,title,text}:{icon:typeof Gauge;title:string;text:string}) { return <div className="rounded-2xl border border-white/8 bg-white/[.025] p-4"><Icon className="h-4 w-4 text-yellow-300"/><p className="mt-3 text-sm font-medium text-stone-200">{title}</p><p className="mt-2 text-xs leading-relaxed text-stone-500">{text}</p></div> }
